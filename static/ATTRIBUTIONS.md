# Asset attributions

All committed binary assets must be CC0 / public-domain or otherwise royalty-free,
and listed here even when attribution is legally optional.

## Textures

- **Hardwood floor** — none committed. The floor is generated procedurally at
  runtime (`src/lib/textures/proceduralWood.ts`), so no image asset ships.

## Audio

- **Die clatter** (`static/audio/clatter_01..06.mp3`) — _not yet committed._
  When adding samples, use CC0 sources only (Freesound CC0 filter, Kenney,
  Pixabay, OpenGameArt) and record each file's source URL and license below.
  Until then `playRandomHit` is a silent no-op (see `src/lib/audio/clatter.ts`).

  | File | Source URL | License | Author |
  |------|-----------|---------|--------|
  | _pending_ | | | |
